What it is ?
------------
	std_timer.c is an example to set standered timer in linux kernel modules. Timer can be adjusted using TIME constant.

How to run ?
------------
	There is a make file which will produce the kernel object file. That can be inserted into the kernel using insmod and removed using rmmod.
	
